#!/bin/sh
# Legacy helper kept for compatibility with older bindings.
# The current image clipboard flow reloads in place after delete.
ueberzugpp cmd -s "$SOCKET" -a exit 2>/dev/null
i3-imgcliphist &
