#!/usr/bin/env bash

pkill -x tte 2>/dev/null || true
pkill -f "$HOME/.local/bin/i3-screensaver" 2>/dev/null || true
pkill -f "alacritty.*--class i3-screensaver" 2>/dev/null || true

brightnessctl set 100% -q 2>/dev/null || true
