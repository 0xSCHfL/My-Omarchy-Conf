#!/bin/bash
url="${1:?usage: i3-webapp URL [profile-directory]}"
profile_dir="${2:-}"

args=(--ozone-platform=x11)
if [[ -n "$profile_dir" ]]; then
  args+=(--profile-directory="$profile_dir")
fi
args+=(--app="$url")

WAYLAND_DISPLAY= GDK_BACKEND=x11 chromium "${args[@]}"
