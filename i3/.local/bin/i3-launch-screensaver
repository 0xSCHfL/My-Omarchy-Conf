#!/bin/bash

command -v tte &>/dev/null || exit 1

# Don't launch if already running
pgrep -f "class i3-screensaver" >/dev/null 2>&1 && exit 0

setsid -f alacritty \
    --class i3-screensaver \
    --config-file "$HOME/.config/alacritty/screensaver.toml" \
    -e i3-screensaver
