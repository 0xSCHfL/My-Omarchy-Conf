# Local Manuals

These are personal manuals for this dotfiles setup. They are opened by:

```bash
i3-manuals
```

The goal is to document how this machine is configured, not generic upstream docs.

Add or edit Markdown files here as the setup evolves.
